# Keep these only in local_studio/.env. Do not commit real credentials.
LOCAL_STUDIO_TOKEN=
INSTAGRAM_ACCESS_TOKEN=
INSTAGRAM_USER_ID=
INSTAGRAM_API_VERSION=v23.0
TIKTOK_ACCESS_TOKEN=
TIKTOK_APP_AUDITED=false
YOUTUBE_ACCESS_TOKEN=

# Optional OAuth *app* identifiers. Operator registers the app; this repo does not.
# Publish still uses the access tokens above. Empty names are inventory only —
# never commit real client IDs, secrets, or tokens.
# GitHub device flow reads GROK_CREW_GITHUB_CLIENT_ID from the Electron process
# (shell / CI / desktop env), not only this sidecar .env.
GROK_CREW_GITHUB_CLIENT_ID=
INSTAGRAM_APP_ID=
INSTAGRAM_APP_SECRET=
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=
GOOGLE_OAUTH_CLIENT_ID=

WHISPER_CPP_BINARY=
WHISPER_CPP_MODEL=

# Optional: how many live MoviePy preview composites to keep (default 4, max 8).
# Switching projects reuses a slot instead of rebuilding. Final render never uses this.
LOCAL_STUDIO_PREVIEW_CACHE_SLOTS=

# Optional: how many render jobs studio_server.py processes at once (default 1,
# meaning renders queue and run one at a time). Raising this trades CPU/RAM
# headroom for shorter queue waits when multiple jobs are started close together.
LOCAL_STUDIO_RENDER_WORKERS=

# Optional: comma-separated browser origins allowed to call the local API and
# receive CORS headers (default: http://localhost:3000,http://127.0.0.1:3000).
# Only set this if you serve the browser workspace from a different port/host.
LOCAL_STUDIO_ALLOWED_ORIGINS=

# Remote bots drop packages here (workspace/handoff-inbox) or on the git
# remote below. The operator writes a spec; the bot supplies source + cut.
# Desktop "Receive the bot cut" imports the local inbox. The watcher still
# polls git when these are set.

# Optional: only used by local_studio/handoff_watcher.py (cloud bot handoff).
# Not a secret — just where your dedicated handoff repo/branch lives.
# These must match the "channel" block in local_studio/handoff-guide.json —
# if you edit one, edit the other. Left blank/default, the watcher polls a
# "handoff-inbox" branch on THIS repo's own origin, which is safe (that
# branch normally doesn't exist) but won't receive anything until you either
# fill these in or create that branch yourself. Example matching the shipped
# handoff-guide.json:
#   HANDOFF_REPO_REMOTE=https://github.com/NoLucas/handoff-inbox.git
#   HANDOFF_BRANCH=main
HANDOFF_REPO_REMOTE=
HANDOFF_BRANCH=handoff-inbox

# Optional caps for local_studio/handoff_watcher.py (see handoff-guide.json's
# "limits" block for the values advertised to a remote bot).
HANDOFF_MAX_MEDIA_BYTES=
HANDOFF_MAX_BUNDLE_BYTES=
HANDOFF_MAX_PACKAGES_PER_CYCLE=
