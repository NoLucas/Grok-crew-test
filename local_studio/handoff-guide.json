{
  "schema": "local-video-workspace.handoff-guide/v1",
  "purpose": "A contract for a remote or cloud-hosted bot that cannot reach Local Studio's loopback API directly. The operator only writes an edit spec. The bot supplies the source video and the cut, then hands both off through a dedicated git branch — never a network call to this PC.",
  "roles": {
    "operator": "Write the spec only (title, goal, length, captions, look). Choose one door: editor (folder editor/) or collector / other-agent (folder collector/). Saving puts spec.json in that door's outbox. Do not attach footage.",
    "grok_door": "Editor door. Folder name is editor/. Leftover grok/ folders are still read. Whoever is assigned as editor. Runner pairing is this door only. Do not use collector/.",
    "other_agent_door": "Collector / other-agent door. Folder name is collector/. Leftover agents/ folders are still read. Whoever is assigned as collector, or a non-crew other-agent. Do not use editor/ or Runner pairing.",
    "bot": "Read spec.json from your door's outbox, find or make the source, edit it, and push a package that includes the video file plus bundle.json with edit_spec_id and project.door."
  },
  "style_recipes": {
    "purpose": "A named pack fills length, aspect, hook, pacing, captions, look, and collect hints. It is not a door.",
    "ids": ["instagram_reel", "tiktok_tight", "youtube_short", "youtube_long"]
  },
  "source_modes": {
    "collect": "Collector gathers clips. Both outboxes. Status waiting_for_collector.",
    "own": "Operator files only. Editor outbox only. Status waiting_for_editor.",
    "own_and_collect": "Operator A-roll plus collector b-roll. Both outboxes. Keep owned clips."
  },
  "crew": {
    "purpose": "One spec, two bots. The collector gathers source clips. The editor cuts those clips. This desk does not scrape websites.",
    "collector": {
      "door": "collector",
      "outbox": "local_studio/workspace/handoff-outbox/collector/{edit_spec_id}/",
      "drop": "local_studio/workspace/handoff-materials/{edit_spec_id}/",
      "git_drop": "materials/{edit_spec_id}/",
      "files": ["manifest.json", "source clips"],
      "clip_fields": ["file", "origin (owned|collected|bundled-sample)", "license (operator|stock|public|unknown)", "source_url"],
      "do_not": "Do not put a finished cut in any inbox. Collect only sources the operator may use. Do not scrape login-walled Instagram or TikTok. Do not replace owned clips."
    },
    "editor": {
      "door": "editor",
      "outbox": "local_studio/workspace/handoff-outbox/editor/{edit_spec_id}/",
      "read": "local_studio/workspace/handoff-materials/{edit_spec_id}/manifest.json",
      "return": "local_studio/workspace/handoff-inbox/editor/",
      "git_return": "editor/"
    }
  },
  "outbox": {
    "purpose": "The operator saves a spec. This PC writes spec.json and brief.txt into that door's outbox. The bot picks up only its door. It never calls 127.0.0.1. After the matching inbox receives the cut, the outbox folder moves to .processed/.",
    "schema": "grok-crew.outbox-spec/v1",
    "local_root": "local_studio/workspace/handoff-outbox/",
    "git_root": "outbox/",
    "files": ["spec.json", "brief.txt"],
    "rule": "Read only your door. Return the finished package to the matching inbox (editor/ or collector/), never into outbox/."
  },
  "doors": {
    "editor": {
      "outbox": "local_studio/workspace/handoff-outbox/editor/{edit_spec_id}/",
      "git_outbox": "outbox/editor/{edit_spec_id}/",
      "inbox": "local_studio/workspace/handoff-inbox/editor/",
      "git_prefix": "editor/",
      "bundle_project_door": "editor",
      "created_by": "the assigned editor name",
      "runner": true
    },
    "collector": {
      "outbox": "local_studio/workspace/handoff-outbox/collector/{edit_spec_id}/",
      "git_outbox": "outbox/collector/{edit_spec_id}/",
      "inbox": "local_studio/workspace/handoff-inbox/collector/",
      "git_prefix": "collector/",
      "bundle_project_door": "collector",
      "created_by": "the assigned collector or other-agent name",
      "runner": false
    }
  },
  "why_this_exists": "Local Studio binds to 127.0.0.1 only and is not reachable from another machine or container by design (see README.md and CONTRIBUTING.md). This channel is an offline handoff, not a remote API — the only thing this repository accepts from a remote bot is a git push to the branch named below.",
  "channel": {
    "medium": "git",
    "repository": "https://github.com/NoLucas/handoff-inbox.git",
    "branch": "main",
    "rule": "Push commits only to this repository's main branch. Never attempt to reach an IP address or hostname belonging to the local operator's machine — this repository is the only sanctioned channel.",
    "folder_naming": "Outbound specs live under outbox/editor/{edit_spec_id}/ or outbox/collector/{edit_spec_id}/ (spec.json + brief.txt). Inbound cuts stay under editor/ or collector/, named '<UTC timestamp>-<short-slug>' (e.g. editor/2026-08-23T07-19-00Z-payoff-hook). Leftover grok/ and agents/ inbound folders are still read. Never put media under outbox/. An editor pull never imports collector/, and a collector pull never imports editor/."
  },
  "limits": {
    "media_extensions": [".mp4", ".mov", ".mkv", ".avi", ".webm"],
    "media_max_bytes": 2147483648,
    "media_max_bytes_note": "2 GB by default; the operator can raise or lower this with HANDOFF_MAX_MEDIA_BYTES in local_studio/.env. A package with an unsupported extension or an oversized file is skipped entirely, not truncated.",
    "bundle_max_bytes": 4194304,
    "bundle_max_bytes_note": "4 MB by default (HANDOFF_MAX_BUNDLE_BYTES). bundle.json is metadata/EDL/artifacts only — it should never need to be this large.",
    "packages_per_cycle": 5,
    "packages_per_cycle_note": "The watcher processes at most this many pending folders per poll (HANDOFF_MAX_PACKAGES_PER_CYCLE via --max-per-cycle, if the operator changes it). Extra submissions simply wait for a later cycle — do not resubmit or assume a delay means failure."
  },
  "package_contents": {
    "bundle_json": "A file named bundle.json at the folder root, using the same schema Local Studio's own project-bundle export/import already uses: local-video-workspace.project-bundle/v1.",
    "bundle_json_shape": {
      "schema": "local-video-workspace.project-bundle/v1",
      "project": {
        "title": "string",
        "source_path": "workspace-relative path this media will be copied to, e.g. inputs/handoff/<folder-name>/source.mp4",
        "output_path": "workspace-relative path for the render, e.g. outputs/handoff/<folder-name>.mp4",
        "timeline": { "clips": [{ "in": 0, "out": 1.4, "keep": true, "caption": "string", "word_timings": "optional, see bot-guide.json" }], "render_settings": "optional, same fields Production's Finish Rack uses (platform, quality, caption_bg, music_track, ...)" },
        "caption": "Instagram caption text, up to 2200 characters",
        "edit_spec_id": "required when fulfilling an operator spec — copy the id from the brief the operator sent",
        "door": "required: editor or collector. grok and agent are accepted as aliases. Must match the inbox folder and the spec.",
        "created_by": "How the desk labels the sender. Use the assigned bot name — the name that checked in, or the name the operator typed. Do not assume Grok or Claude.",
        "handoff_auto_upload_requested": "boolean, optional — see auto_upload_policy below"
      },
      "jobs": [
        { "kind": "render", "approved": true, "payload": {} },
        { "kind": "instagram_publish", "approved": true, "payload": { "auto_upload": true } }
      ],
      "artifacts": []
    },
    "media_file": "The bot must include the actual video file. The operator does not provide it. Place it at the folder root using only the final path segment (source.mp4). Extra B-roll files listed on timeline.assets[].path go in the same folder.",
    "rule": "Omit a job's 'status' field entirely so it is created fresh as queued+approved. Do not set 'status' to succeeded/failed unless you are intentionally replaying history, not requesting new work."
  },
  "auto_upload_policy": {
    "rule": "Setting bundle.project.handoff_auto_upload_requested: true and jobs[].payload.auto_upload: true only requests immediate Instagram publishing. It has no effect unless the PC's operator has separately started local_studio/handoff_watcher.py with --allow-auto-upload. Both conditions are required.",
    "default": "Without that local opt-in, the instagram_publish job is created and left queued; a person runs it from the Production page when ready.",
    "render_is_different": "A render job only produces a local MP4 file — it has no external effect — so it runs automatically once approved: true is set, independent of --allow-auto-upload."
  },
  "processing": "A background script on the operator's PC (local_studio/handoff_watcher.py) polls this branch, copies the media into its own workspace, and calls its own POST /api/projects/import with bundle.json verbatim. Processed folders are normally removed from the branch afterward — do not assume a folder stays on the branch as a permanent record.",
  "never": [
    "Never attempt to connect to 127.0.0.1, localhost, or any LAN/WAN address for this project — only the git branch above is a sanctioned channel.",
    "Never request, embed, or guess LOCAL_STUDIO_TOKEN, INSTAGRAM_ACCESS_TOKEN, or any other credential in a package.",
    "Never push to any repository or branch other than the one named in 'channel'.",
    "Never assume a submission was applied — this channel has no delivery confirmation beyond the operator's own local logs."
  ]
}
